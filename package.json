{
  "name": "phonebookbackend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd /Users/aaron/Desktop/Dev/Full-Stack-Open/part3/phonebookfrontend && npm run build && cp -r dist /Users/aaron/Desktop/Dev/phonebookbackend",
  "deploy:full": "npm run build:ui && git add . && git commit -m 'rebuild' && git push"

  },
  "author": "Aaron Taylor",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
